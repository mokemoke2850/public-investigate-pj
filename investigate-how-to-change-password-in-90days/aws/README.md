1. Create zip file with the following command:

```bash
cd lambda/change-cognito-password-status
npm install
npm run build
```

2. Deploy the resources with the following command:

```bash
terraform init
terraform apply
```
