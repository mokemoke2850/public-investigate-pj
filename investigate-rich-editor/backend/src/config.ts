export const PORT = 3000;
export const HOST_NAME = 'localhost';
export const PROTOCOL = 'http';
export const API_PREFIX = '/api';
export const BASE_URL = `${PROTOCOL}://${HOST_NAME}:${PORT}${API_PREFIX}`;
